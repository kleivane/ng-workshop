// Legge til appfolders

// Lage en index.html med link til oppgave 1 
// Legge til et unicorn directive i index

// Grunttasks
// package : (hint) ng-template, concat,
// ng-template, copy(?), server, open, watch  

// Installasjonsbeskrivelse i README

// CSS?